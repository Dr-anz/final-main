name: Test and Publish to Docker Hub

on:
  push:
    branches: [ first-iteration ]  # триггер при пуше в вашу ветку
    tags: [ 'v*.*.*' ]           # триггер при добавлении тегов вида v1.0.0
  pull_request:
    branches: [ first-iteration ]  # триггер при PR в вашу ветку

jobs:
  test-code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Run tests
        run: go test -v ./...

      - name: Build binary
        run: go build -o app .

  publish-to-dockerhub:
    runs-on: ubuntu-latest
    needs: test-code
    if: contains(github.ref, 'tags')  # выполняется только при наличии тега
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/final-main:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/final-main:${{ github.ref_name }}
